rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global access for sandboxed preview compatibility
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

